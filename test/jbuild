(jbuild_version 1)

(executable
 ((name test)
  (preprocess (pps (ppx_let)))
  (libraries (alcotest alcotest-async thread_pool))))

(alias
 ((name runtest)
  (deps ((file test.exe)))
  (action (run ${<}))))
